# RGB-D 候选版本检查：2026-09-05

## 已验证结果

固定场景 smoke：1/1 物理成功。此结果仅作为进入重复性实验的闸门，不作为发布成功率。

- 目标初始 XY：`(0.520, 0.000) m`。
- 目标放置 XY：`(0.450, -0.080) m`。
- 抬升：126.1 mm。
- 最终水平放置误差：3.6 mm。
- 最终倾角：约 0.0001 度。
- 总试验耗时：108.9 s，包含启动与验证。
- 流水线终态 SUCCESS；日志中释放、撤离、return_home 均执行成功。
- 独立 Gazebo validator 确认抬升与直立放置成功。

## 配置边界

本次使用 `--rgbd-perception --seed 42`，默认规划器回退链，下降后端为 Cartesian。可视化 `rgbd_markerless_pick.launch.py` 则固定 RRTConnect；发布前需明确统一候选配置，不能将两入口的数据视为完全同配置复现。

后续配置修正：已移除可视化入口额外写死的 RRTConnect，沿用父 launch 的回退链和下降后端。安装后的 `--show-args` 已成功解析并显示三规划器回退链与默认 Cartesian；单规划器可通过 `planner_ids_csv` 显式设置。该修改后的重复试验另存为 `release_rgbd_fixed3_20260905`，不覆盖上面的单次结果。

本次并非 Servo 下降验收；Servo 专项证据单独保留。视觉输入由 RGB-D 提供，在线物理监测与放置补偿中使用的 Gazebo 真值依赖仍需在发布材料中披露。

## 证据目录

`artifacts/runs/release_rgbd_gate_20260905/`：

- `source_manifest.csv`：运行前记录的 102 个源码、脚本、配置和测试文件 SHA256。
- `results.csv`、`report.md`、`report.svg`：完整 benchmark 输出。
- `logs/trial_001.log`：实际 launch 参数、各步骤状态和 validator 原始结果。

当前本地 Git 尚无提交，指纹清单仅标识记录中的文件，不能替代最终发布提交或完整环境锁定文件。该清单不包含 Dockerfile 与系统依赖版本。

## 下一验收闸门

### 统一入口后的固定三次回归

`artifacts/runs/release_rgbd_fixed3_20260905/` 已完成 3/3 物理成功，无基础设施启动失败。平均抬升 127.1 mm，平均放置误差 3.0 mm（标准差 1.1 mm），平均总耗时 104.60 s。Wilson 95% 成功率区间为 43.9%-100.0%，仅证明小样本闸门通过。

批次内使用固定目标 `(0.52, 0.00)`、RGB-D 感知、默认规划器回退链和 Cartesian 下降，未调整控制阈值。批次期间记录的源码指纹与结束后核对一致；清单在批次运行期间生成，并非运行前完整环境快照。CSV、报告、图表和三份日志保存在上述独立目录。

下一步以此配置进入固定 10 次正式回归，再运行随机 20 次。该三次批次单独保留，不并入正式批次以替换失败。

先固定演示与评测使用的规划器/下降后端，再完成小样本重复验证。通过后按发布计划运行固定 10 次和随机 20 次；所有失败计入对应批次，任何源码或关键参数变化另起批次。
